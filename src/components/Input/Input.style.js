import { StyleSheet } from "react-native";

export default StyleSheet.create({
    container: {
        padding: 10,
        margin: 10,
        borderRadius: 5,
        flexDirection: 'row',
        backgroundColor: 'white',
        borderWidth: 0.5,
    },
    input: {
        flex: 1,
    },
})